import { Router } from "express";

// === GET/PUT /api/thinkers — the roster of thinkers ===
//
// Stub. `onda2-roster-routes` fills it, over `services/thinker-roster.ts`.
//
// It exists ahead of the implementation for the same reason `models.ts` does:
// so the route registration in `index.ts` is written once, before the wave.

const router = Router();

export default router;
